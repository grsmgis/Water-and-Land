SET up:
    Install netcdf libray for c
        Ubuntu:
            ```sudo apt-get install libnetcdf-dev libnetcdff-dev```
    Make libfdr libaray:
        ```cd libfdr```
        ```make ```
    
    